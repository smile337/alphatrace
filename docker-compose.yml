services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: alphatrace
      POSTGRES_USER: alphatrace
      POSTGRES_PASSWORD: alphatrace
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

volumes:
  postgres-data:
